export let one = 10;
export let two = 20;
export const sing = function () {
  console.log("唱,跳,Rap");
};
// export default {
//   one,
//   two,
//   sing,
// };

// 默认导出，必须使用默认导入来接收
// 按需导出，必须使用按需导入来接收
